# 子供用「くるまドライブ」PWA 実装プロンプト(Claude Code用)

このファイルをそのままClaude Code(CLI)に渡して実装を依頼してください。
下調べと設計判断はすでに済ませてあります。Claude Codeはこの仕様に沿って
実装・仕上げ・動作確認を行ってください。

## 1. 作りたいもの

子供向けの3Dドライブ PWA(Progressive Web App)。

1. 好きな車を選ぶ画面(Kenney製の車モデル25種類から選択)
2. 選んだ車で1つのコースを走る画面
   - **手動**: キーボード(矢印/WASD)またはタブレット/スマホの画面上ボタンで運転
   - **オート**: ボタン一つでコースを自動周回するデモ走行に切り替え可能
3. ホーム画面に追加してオフラインでも起動できるPWAにする

対応端末: **PCとタブレット/スマホの両方**。タッチデバイスかどうかを自動判定し、
タッチデバイスでは画面上に大きな操作ボタンを表示、PCではキーボード操作を基本とする
(キーボードは常時有効でよい)。

## 2. 使用する素材(すでに調査済み)

ユーザーは以下2つのKenneyアセット(どちらもCC0 / パブリックドメインライセンス、
クレジット表記は任意)をアップロード済みです。zipファイルをプロジェクトの
`assets_source/` に置いて展開してください。

### `kenney_car-kit.zip`
- `Models/GLB format/*.glb` にブラウザでそのまま読み込めるGLB形式の車モデルがある
- `Previews/*.png` に各モデルの見た目そのままのサムネイル画像がある(選択画面にそのまま使える)
- GLBはDraco/meshopt圧縮なし。追加の圧縮ライブラリは不要で`GLTFLoader`だけで読み込める
- `License.txt` に CC0 と明記

**選択画面で使う25車種**(id = ファイル名、表示名は例。調整OK):

| id | 表示名 |
|---|---|
| race | レーシングカー |
| race-future | 未来のレースカー |
| sedan-sports | スポーツセダン |
| hatchback-sports | スポーツカー |
| sedan | セダン |
| suv | SUV |
| suv-luxury | 高級SUV |
| taxi | タクシー |
| police | パトカー |
| tractor-police | パトロールカー |
| ambulance | 救急車 |
| firetruck | 消防車 |
| van | バン |
| delivery | 配達トラック |
| delivery-flat | 配達バン |
| truck | トラック |
| truck-flat | 大型トラック |
| garbage-truck | ごみ収集車 |
| tractor | トラクター |
| tractor-shovel | ショベルカー |
| kart-oobi | ゴーカート(あお) |
| kart-oodi | ゴーカート(みどり) |
| kart-ooli | ゴーカート(きいろ) |
| kart-oopi | ゴーカート(あか) |
| kart-oozi | ゴーカート(オレンジ) |

(このほか `wheel-*` `debris-*` `box.glb` はパーツ用/がれき用なので選択肢に含めない。
`cone.glb` はコースの飾り付け用に使える)

### `kenney_3d-road-tiles.zip`
- 302個の道路・歩道・水辺タイル(`roadTile_001.obj`〜`roadTile_302.obj`、GLTF版も同梱)
- **重要な注意**: これらは連番になっているだけで、どれが直線でどれがカーブ・交差点かを示す
  索引ファイルが無く、色もSketchUp書き出しのフラット頂点カラー(道路・歩道・水面・芝生などが
  混在)。プログラムだけで正しく組み合わせてループコースを自動生成するのは非常に手間がかかり、
  壊れやすい。
- **推奨**: このタイル集を実際にパズルのように組み合わせるのではなく、
  「Kenneyのフラットカラー・ローポリな見た目」を再現した**コースを自作のジオメトリで生成**する
  (下記セクション4参照)。CC0ライセンスなので、余裕があれば後から気に入ったタイル番号を
  Previewから目視で選んで飾りに使ってもよい(必須ではない)。

## 3. 画面構成

1. **ローディング画面**: 簡単なアニメーション+プログレスバー
2. **車選択画面**: 25台をグリッド表示(`Previews/*.png`をサムネイルに使用)、
   タップ/クリックで選択 → 「スタート」ボタンで決定
3. **ゲーム画面**:
   - 3Dコースと選んだ車を表示
   - HUD: 戻る(ホーム)ボタン、オート/マニュアル切替ボタン、サウンドON/OFFボタン、車名表示
   - タッチデバイスのみ: 左右ステア・アクセル・ブレーキの大きな半透明ボタンを画面下に表示

## 4. コース(トラック)の作り方(推奨アルゴリズム)

タイルを敷き詰めるのではなく、**閉じたスプライン曲線から道路メッシュを生成**する方法を
推奨します。理由: 必ず綺麗に繋がったループになる/オートモードの経路追従にそのまま使える/
実装が壊れにくい。

手順:
1. `THREE.CatmullRomCurve3(points, true, 'catmullrom', 0.55)` で閉じた曲線を作る
2. 曲線を400分割程度でサンプリングし、各点で進行方向に垂直な向き(道路の左右方向)を求める
3. 道幅(例: 11ユニット)ぶん左右にオフセットしたエッジ座標を計算し、
   帯状の三角形ストリップ(`BufferGeometry`)を作る → これが路面
4. 同様に外側へさらにオフセットした帯を作って縁石(色違い)にする
5. 中央線は`InstancedMesh`で細長い直方体を等間隔に並べて破線にする
6. 地面は大きな円(`CircleGeometry`)に芝生色マテリアル
7. スタート/ゴールにゲート(2本のポール+横断幕)を置く
8. Car Kitの`cone.glb`をコース脇に点在させる、簡単な低ポリの木(円錐+円柱)を外周に散らすと
   賑やかになる(必須ではないが子供向けとして効果的)

**コースの制御点(そのまま使ってOK。単位はメートル相当、道幅11)**:

```js
const points = [
  [0, -46], [26, -50], [48, -38], [56, -14], [50, 10],
  [30, 22], [30, 40], [10, 52], [-18, 48], [-34, 30],
  [-30, 8], [-46, -4], [-54, -26], [-38, -46], [-16, -50],
].map(([x, z]) => new THREE.Vector3(x, 0, z));
```

## 5. 車の操作・物理(アーケード感でOK。本格的な車両物理は不要)

- 前進最高速度・後退最高速度・加速度・ブレーキ減速度・自然減速(惰性)・最大旋回速度を
  シンプルな数式で実装する
- ハンドルは**速度に応じて効きを変える**(止まっている時はほぼ曲がらない、走っているときだけ
  よく曲がる)と自然に感じる
- コース(道路)の外(芝生)に出たら速度に軽くペナルティ(ドラッグ)をかけると
  「コースを走る」意味が出て良い(コースアウトで即クラッシュ、のような厳しい失敗は
  子供向けなので避ける)
- 車種ごとに速度係数・旋回係数を少し変えると個性が出る(例: ゴーカートは速くてよく曲がる、
  トラック系は遅くて曲がりにくい)

**目安の数値**:
```
最高速度: 26 units/秒 (車種係数 0.7〜1.3倍)
後退最高速度: 10 units/秒
加速度: 22 units/秒^2
ブレーキ減速度: 34 units/秒^2
自然減速: 10 units/秒^2
最大旋回速度: 2.4 rad/秒
オート走行の巡航速度: 14 units/秒(一定)
コース外ドラッグ係数: 0.45倍
```

**オートモード**は、コース曲線上の進行度 `u`(0〜1)を一定速度で進め、
`curve.getPointAt(u)` で位置、`curve.getTangentAt(u)` で進行方向を取得して
車の位置・向きをそのままセットすると簡単に実装できる。手動⇔オートの切り替え時は、
現在位置に一番近いコース上の`u`を探してから開始するとスムーズ。

## 6. 車モデルの向きに関する注意(要実機確認)

Car KitのGLBモデルの「正面(前進方向)」がThree.jsの-Z軸と一致していない可能性があります。
実装後、実際にブラウザで前進操作をして車が鼻先の方向に進むか確認し、ズレていたら
モデルをラップした`Group`に`rotation.y`のオフセット(0か`Math.PI`のどちらか)を
追加して補正してください。

## 7. PWA化のポイント

- `manifest.json`: `name`, `short_name`, `start_url`, `display: "standalone"`,
  `theme_color`, `background_color`, `icons`(192px/512px、maskable推奨)
- Service Worker: `index.html`・CSS・JS・25台ぶんのGLB・サムネイル画像・
  three.jsのモジュールファイルをinstall時に全てプリキャッシュし、オフラインでも
  車選択〜走行まで一通り遊べるようにする
- three.jsは **CDNではなくローカルにバンドル**(`npm install three`で取得した
  `build/three.module.js`と`examples/jsm/loaders/GLTFLoader.js`、
  `examples/jsm/utils/BufferGeometryUtils.js`をプロジェクトにコピーして
  相対importする)。オフライン時にCDNへの接続が必要にならないようにするため
- アイコン画像は用意されていないので、車のPreview画像や簡単な図形から作成する

## 8. 添付の「たたき台」について

このプロンプトと一緒に、途中まで実装したファイル一式(zip)を渡しています。
中身:
- `assets/models/cars/*.glb` … 25台の車モデル(コピー済み)
- `assets/models/decor/cone.glb` … コーン(コピー済み)
- `assets/previews/*.png` … 選択画面用サムネイル(コピー済み)
- `lib/three/...` … three.js本体とGLTFLoader(npm経由で取得済み、CDN不使用)
- `js/cars.js` … 車種データ(上記の表と同じ内容)
- `js/track.js` … 上記アルゴリズムでコースを生成する実装(そのまま使える想定)
- `js/carController.js` … 上記の物理・操作・オートモードの実装(そのまま使える想定、
  ただし前述の向き補正は要確認)
- `index.html` / `css/style.css` … 画面のHTML/CSS(ローディング・選択・ゲーム画面、
  タッチボタンのレイアウト込み)

**まだ無いもの(Claude Codeで作成してほしいもの)**:
- `js/main.js` … 上記の各モジュールを繋ぎ合わせて実際に動かすメインスクリプト
  (Three.jsのシーン/カメラ/レンダラー初期化、車選択画面のクリック処理、
  キーボード/タッチ入力の取得、毎フレームの`carController.update()`呼び出し、
  三人称追従カメラ、GLTFLoaderでの車モデル読み込みなど)
- `manifest.json`
- Service Worker(`sw.js`)とその登録処理
- アプリアイコン(192px/512px)
- 動作確認・デバッグ・調整(特に車の向き補正、当たり判定の感触、タッチボタンの
  使い勝手)

## 9. 完成の確認項目

- `npx serve` 等でローカル配信し、Chrome DevToolsのLighthouseでPWA要件(installable等)
  を満たしているか確認
- 実機のタブレット/スマホでホーム画面に追加し、機内モードでも起動〜プレイできるか確認
- 25台すべて問題なく読み込めるか確認
- 選択→スタート→手動運転→オート切替→手動に戻す、の一連の流れが破綻なく動くか確認
- 画面サイズ(スマホ縦持ち・タブレット・PC)を変えてレイアウトが崩れないか確認

---
以上の内容に沿って実装してください。判断に迷う細部(色味・数値のチューニングなど)は
子供が楽しく遊べることを優先して適宜調整して構いません。
